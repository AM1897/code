import React from "react";
import './Produkter.css'

const Produkter = ({albumItems}) => {
  return(
      <div className='produkter'>
          {albumItems.map((albumItems) => ()
          }
      </div>
  )
}
export default Produkter